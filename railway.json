{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "cd apps/api && npx prisma db push --accept-data-loss --skip-generate && cd /app && node apps/api/dist/src/main.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
