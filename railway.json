{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss --schema=apps/api/prisma/schema.prisma && node apps/api/dist/src/main.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
