# Database
DATABASE_URL=postgresql://deckpilot:deckpilot_dev@localhost:5432/deckpilot
DB_USER=deckpilot
DB_PASSWORD=deckpilot_dev
DB_NAME=deckpilot
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# MinIO (S3-compatible storage)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_USE_SSL=false
MINIO_BUCKET=deckpilot-files

# JWT
JWT_SECRET=change-me-in-production-use-a-long-random-string
JWT_REFRESH_SECRET=change-me-in-production-use-a-different-string
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# OpenAI (for embeddings in Phase 2)
OPENAI_API_KEY=sk-...

# Replicate (for image generation in Phase 6)
REPLICATE_API_TOKEN=r8_...

# Stripe (for billing in Phase 5)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# App
PORT=3000
FRONTEND_URL=http://localhost:5173
NODE_ENV=development
